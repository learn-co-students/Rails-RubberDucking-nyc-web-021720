<h1>These are some cool students:</h1>

<ul>
  <% @students.each do |student| %>
    <li> <%= student.name %> | mod: <%= student.mod %>
    <%= button_to "Delete", student_path(student), method: :delete %> 
    <%= button_to "Edit", edit_student_path(student), method: :get%> 
    </li>  
 <% end %>
  
</ul>

<%= link_to "Home", students_path %> |
<%= link_to "Create", new_student_path %>
